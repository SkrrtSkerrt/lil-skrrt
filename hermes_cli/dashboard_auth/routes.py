"""Dashboard auth routes placeholder.

The main web server imports this router unconditionally. The compatibility
package keeps the import path available even when the full OAuth provider
stack is not installed.
"""

from __future__ import annotations

from fastapi import APIRouter

router = APIRouter()
